# Optional: dùng role có sẵn (vd. newtofu không có quyền tạo IAM role)
# LAMBDA_ROLE_ARN=arn:aws:iam::ACCOUNT_ID:role/LambdaS3ReadRole

# Optional: ghi đè mặc định
# BASE_URL=https://www.springsummer2026margiela.com
# SLACK_WEBHOOK_URL_E2E=https://hooks.slack.com/...
# AWS_REGION=ap-east-1
